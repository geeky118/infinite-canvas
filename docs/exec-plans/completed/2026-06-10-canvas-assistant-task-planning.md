# 画布助手生图状态和任务拆分优化

## 目标

- 修复图片接口成功但助手消息显示“请求失败”的体验。
- 对多图、多姿势、多页面 UI 设计等请求自动拆成独立图片任务，每个任务单独生成 1 张图。
- 保持“使用上文提示词”能够继承上一轮文字对话生成的提示词。

## 实施步骤

- [x] 梳理画布助手生图、上传、插入画布的状态链路。
- [x] 增强生图结果处理，避免后处理失败覆盖已返回的图片结果。
- [x] 增加通用图片任务编排，支持意图分析、角色分配、多姿势、多页面、多方案自动拆分。
- [x] 将图像任务执行层统一为 JSON：规划智能体输出 `execution` 对象，实际生图/修图请求使用结构化 JSON 提示词；多交付物强制每个任务 1 张，单交付物候选图保持任务 `count` 和 `execution.output.count` 一致。
- [x] 更新待测文档并做针对性验证。

## 验证记录

- `cd web && npx tsc --noEmit --pretty false` 通过。
- `cd web && npm run build` 通过。
- 补充执行层 JSON 后，重新执行 `cd web && npx tsc --noEmit --pretty false` 通过。
- 补充执行层 JSON 后，重新执行 `cd web && npm run build` 通过。
- 补充执行层 JSON 后，已重新 frontend-only 发布到测试服务器 `https://canvas.hello4am.com`。
- 线上 `/canvas` 返回 200，容器 `chatgpt2api-prod-canvas` 健康。
- 线上 `/image-proxy` 读取历史生图 URL 返回 `200 image/png`。
- 线上新建画布会打开项目系统提示词弹窗，关闭后可打开画布助手，控制台无错误。
- 线上助手面板收起按钮存在；采样耗时：画布 DOM 加载约 1.1s，网络静默约 5.2s，新建画布约 0.9s，助手展开约 0.1s。
- 线上 Playwright 拦截式端到端验证通过：模拟规划器返回“首页/商品详情页”两个 JSON 子任务后，前端实际发起 2 次 `/api/v1/images/generations` 请求，每次 `n=1`，请求 `prompt` 可解析为 JSON，`output.count=1`，对话结果显示已拆分生成 2 张并自动添加到画布空白位置。
- 补充参考图多场景拆分、安全重试和残留引用过滤后，重新执行 `cd web && npx tsc --noEmit --pretty false` 通过。
- 补充参考图多场景拆分、安全重试和残留引用过滤后，重新执行 `cd web && npm run build` 通过。
- 已重新 frontend-only 发布到测试服务器 `https://canvas.hello4am.com`，容器 `chatgpt2api-prod-canvas` 重建后运行。
- 使用真实账号 `1079132971@qq.com` 在“原画创作”画布执行“生成一个火影忍者雏田的写真，2-3张”：线上实际发起 3 个并行 `/v1/images/generations` 请求，均为 `200`，每个请求 `n=1`、`prompt` 为 JSON、`output.count=1`，画布新增 3 个图片节点。
- 使用同一画布连续执行“生成海贼王娜美角色写真照片，坐着，站着，跪着，每个姿势一张图”：线上实际发起 3 个并行 `/v1/images/generations` 请求，标题分别为坐姿写真、站姿写真、跪姿写真，均为 `200`，画布新增 3 个图片节点；同时验证连续生图不会把上一轮残留选中图片误带入请求，未再错误走 `/v1/images/edits`。
- 使用刚生成的娜美坐姿图作为参考执行“保持人物脸型、发色、服装和整体画风一致，生成至少3张其他场景下的写真图，每个场景一张图”：线上实际发起 3 个并行 `/v1/images/edits` 请求，均为 `200`，画布新增 3 个图片节点，任务场景为海边甲板、城市街头、咖啡馆。
- 本轮线上测试期间控制台仍有历史资源 `net::ERR_FILE_NOT_FOUND`，来自画布已有旧节点资源，不影响本次助手请求、图片生成、图片编辑和新增节点插入链路。
